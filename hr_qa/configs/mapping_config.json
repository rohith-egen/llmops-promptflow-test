{
    "experiment": {
        "question": "${data.Question}"
    },
    "evaluation": {
        "hr_evaluation": {
            "groundtruth": "${data.groundtruth}",
            "prediction": "${run.outputs.output}"
        }
    }

}