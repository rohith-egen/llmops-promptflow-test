{
    "experiment": {
        "question": "${data.Questions}"
    },
    "evaluation": {
        "evaluation": {
            "groundtruth": "${data.Answer}",
            "prediction": "${run.outputs.output}"
        }
    }

}